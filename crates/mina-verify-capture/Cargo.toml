[package]
name = "mina-verify-capture"
version = "0.0.1"
edition = "2021"
description = "Capture a live Mina devnet block off the consensus-gossip network."
license = "Apache-2.0"

[dependencies]
libp2p = { workspace = true, features = [
    "tokio",
    "noise",
    "pnet",
    "tcp",
    "yamux",
    "dns",
    "gossipsub",
    "macros",
] }
tokio = { workspace = true }
blake2 = { workspace = true }
hex = { workspace = true }
pin-project-lite = { workspace = true }
env_logger = { workspace = true }
log = { workspace = true }
